pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        maven { url 'https://oss.sonatype.org/content/repositories/snapshots/' }
        gradlePluginPortal()
    }
}

rootProject.name = 'hldemo'

gradle.rootProject { ext.javacppVersion = '1.5.8' }
includeBuild "../.."
